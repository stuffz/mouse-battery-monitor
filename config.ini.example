# Mouse Battery Monitor Configuration

# How often to poll the battery, in seconds
update_interval_seconds = 300

# Show a notification when the battery drops below the threshold
show_notifications = true

# Battery percentage threshold for the low battery warning
low_battery_threshold = 20

# Verbose logging to stdout and the log file
debug_mode = false
